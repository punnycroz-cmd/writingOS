import { readFileSync, writeFileSync } from 'node:fs';
const summary = JSON.parse(readFileSync('writing-engine/logs-golden-v1/summary.json', 'utf-8'));
const ledger = JSON.parse(readFileSync('writing-engine/logs-golden-v1/canonical-case-ledger.json', 'utf-8'));
const REQUIRED = ['activeCases','scorableTriage','triageCorrect','scorableFinal','finalCorrect','semanticScoringEligible','ioOwnershipScorable','ioOwnershipCorrect','faithfulnessScorable','faithfulnessCorrect','falseAcceptance','falseRejection','r6Cases','executionErrors','llmExecuted','deterministicFastPathed'];
const defects = ledger.filter((e:any) => ['KNOWN_DEFECT','REGRESSION','PERSISTENT_DEFECT'].includes(e.historicalComparison));
const tp = summary.scorableTriage>0?Math.round(summary.triageCorrect/summary.scorableTriage*100):0;
const fp = summary.scorableFinal>0?Math.round(summary.finalCorrect/summary.scorableFinal*100):0;
const ip = summary.ioOwnershipScorable>0?Math.round(summary.ioOwnershipCorrect/summary.ioOwnershipScorable*100):0;
const hp = summary.faithfulnessScorable>0?Math.round(summary.faithfulnessCorrect/summary.faithfulnessScorable*100):0;
const rows = REQUIRED.map(k=>`| ${k} | ${summary[k]} |`).join('\n');
const drows = defects.map((d:any)=>{const t=d.isR6?'R6 observation-framed leak':(d.expectedFinalDecision==='REJECT'?'False acceptance':'False rejection');return `| ${d.id} | ${t} | ${d.expectedFinalDecision} | ${d.currentFinalDecision} | ${d.historicalComparison} |`;}).join('\n');
const content = `# Golden Corpus v1 — FROZEN\n\n**Status:** FROZEN  \n**Frozen At:** 2026-08-22 (Phase 2B.5R)  \n**Active Cases:** ${summary.activeCases} (${summary.historicalCases} including ${summary.supersededCases} superseded)  \n**Register:** FICTION  \n**Provider:** FIREWORKS (qwen3p8-max)\n\n## Canonical Metrics (machine-verified)\n\nThese metrics are parsed by \`src/corpus/freeze-document-parser.ts\` and compared\nagainst \`writing-engine/logs-golden-v1/summary.json\` by consistency check #18.\nEvery value below MUST match the corresponding key in summary.json.\n\n| Metric | Value |\n|---|---|\n${rows}\n\n## Frozen Baseline (human-readable)\n\n| Metric | Value |\n|---|---|\n| Scorable triage | ${summary.scorableTriage} | correct: ${summary.triageCorrect} (${tp}%) |\n| Scorable final | ${summary.scorableFinal} | correct: ${summary.finalCorrect} (${fp}%) |\n| Scorable semantic | ${summary.semanticScoringEligible} | io: ${summary.ioOwnershipCorrect}/${summary.ioOwnershipScorable} (${ip}%), faith: ${summary.faithfulnessCorrect}/${summary.faithfulnessScorable} (${hp}%) |\n| False acceptance | ${summary.falseAcceptance} |\n| False rejection | ${summary.falseRejection} |\n| R6 cases | ${summary.r6Cases} (2 improved, 1 persistent) |\n| Execution errors | ${summary.executionErrors} |\n| LLM executed | ${summary.llmExecuted} |\n| Deterministic fast-pathed | ${summary.deterministicFastPathed} |\n\n## Immutability\n\nThis corpus is frozen. Ground truths are immutable. New cases go in golden-corpus-v2. Supersede, don't edit.\n\n## Superseded Cases\n\n| Original | Superseded By | Reason |\n|---|---|---|\n| GC-0038 | GC-0038R1 | Incorrectly tagged as R6/UNRESOLVED (KNOWS-state case, correct=ACCEPT) |\n\n## Known Defects\n\n| Case | Type | Expected | Current | Status |\n|---|---|---|---|---|\n${drows}\n\n## GC-0038R1 Ground-Truth Correction\n\nGC-0038R1's \`expectedSemantic\` was corrected from \`{infoOwnership: FAIL, faithfulness: FAIL}\` to \`{infoOwnership: PASS, faithfulness: PASS}\` because Maya is in the KNOWS state and the observation is consistent with her existing knowledge. This is a corpus maintenance correction, not a model-performance adjustment. GC-0038 (the original) remains SUPERSEDED and unchanged.\n`;
writeFileSync('docs/corpus/GOLDEN_CORPUS_V1_FREEZE.md', content);
console.log('Freeze doc generated.');
